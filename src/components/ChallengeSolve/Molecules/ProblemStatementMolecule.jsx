import React from "react";
import { Typography } from "@mui/material";

const ProblemStatementMolecule = ({ description }) => (
  <Typography variant="body1">{description}</Typography>
);

export default ProblemStatementMolecule;
